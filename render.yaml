services:
  - type: web
    name: aeromesh
    runtime: docker
    dockerfilePath: ./Dockerfile
    envVars:
      - key: PORT
        value: 8000
    disk:
      name: aeromesh-data
      mountPath: /app/outputs
      sizeGB: 1
